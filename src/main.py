def main():
    # duration = self.get_duration(start, end)
    # cmd = 'ffmpeg -ss {start} -i "{input}" -t {t} {codec} "{output}"'
    # return cmd.format(
    #     input=self.input,
    #     start=start,
    #     t=duration,
    #     codec=self.codec,
    #     output=outname,
    # )
    pass


if __name__ == "__main__":
    main()
